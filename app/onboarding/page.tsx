import { OnboardingFlow } from "@/components/onboarding/OnboardingFlow";

export default function OnboardingPage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-[var(--color-brand-50)] to-white px-4 py-10">
      <OnboardingFlow />
    </main>
  );
}
